# Что нового

Коротко о каждом выпуске: что изменилось для того, кто пользуется сервером.
Подробности для разработчиков — в истории коммитов.

## 2.10.17

- Закрыв окно обновления, человек терял из виду, что оно идёт: панель выглядела
  так, будто ничего не происходит. Теперь внизу висит полоса хода, а нажатие
  возвращает окно. Перезапуск панели её не гасит — в этот момент она так и
  говорит, что панель перезапускается.

## 2.10.16

Выпуск целиком про то, как панель говорит с человеком.

- Нажатие «Понятно» в окне итога обновления роняло второе такое же окно —
  исправлено. Заголовок теперь «Обновление установлено», а текст — «Панель
  обновлена до версии X»; крестик убран, кнопки достаточно.
- Окно хода обновления больше не показывает служебную строку шага. Вместо неё
  сказано то, что и нужно знать: обновление идёт на сервере, окно и вкладку
  можно закрыть, результат покажется по возвращении.
- Пункт «Проверить обновления» молчал и пока работал, и о результате. Теперь во
  время проверки он называется «Проверяю», а когда вышла новая версия —
  «Доступна новая версия», и метка стоит рядом с ним, а не отдельно на кнопке
  меню.
- Даты писались числом с точками и в каждом окне по-своему. Теперь всюду
  «11 сен» и «11 сен, 23:50:25»; год добавляется только для записей прошлых лет.
- Окно ограничения скорости обещало то, чего замеры не показывали. Текст стал
  осторожнее, на кнопке появилась вертушка, а крестик во время применения
  недоступен: он обещал бы отмену, которой нет — запрос уже ушёл на сервер.
- Шапка окна уезжала вверх вместе с содержимым при прокрутке; на крестике сразу
  после открытия висело кольцо фокуса. И то и другое исправлено.
- Значения в интерфейсе начинались со строчной буквы и выглядели обрывками фраз:
  «вход», «как у сервера», «офлайн». Теперь с большой.
- В окне «О версиях» строка «2.10.15 — последняя» читалась как вторая,
  незнакомая версия. Когда новее ничего нет, номер стоит один.

## 2.10.15

- На графике появились закрашенные промежутки — время, когда сервер не слышал
  клиента. Нули внутри закраски значат обрыв, нули вне неё — что клиент был на
  связи и просто ничего не качал. Раньше и то и другое выглядело одинаково, и
  отличить их было невозможно. «Неизвестно» не закрашивается, а при наведении
  обрыв назван словами.
- Исправлен сам признак связи, вышедший в прошлой версии. Он определялся по
  возрасту рукопожатия, а оно стареет и у совершенно здорового простаивающего
  клиента: рукопожатие обновляется только при передаче данных. Пятнадцать минут
  без загрузок — и прошлая версия назвала бы это обрывом. Теперь смотрим на
  другое: клиент раз в 25 секунд присылает keepalive, и тишина дольше минуты
  означает обрыв. Точность выросла с трёх минут до одной.

## 2.10.14

- Окно обновления больше не пропадает на середине. Панель обновляет сама себя и
  на шаге установки перезапускает свой контейнер — вкладка на это время теряет
  связь, и раньше окно просто исчезало, а итог всплывал только после ручной
  перезагрузки страницы. Теперь молчание панели считается перезапуском, а не
  неудачей: окно остаётся, говорит, что панель перезапускается, и само
  показывает итог, когда связь вернулась. Если панель не поднялась за три
  минуты, окно честно скажет об этом.
- Заголовок окна во время обновления — «Обновляем…», а под полосой хода теперь
  человеческое пояснение вместо «Шаг: запрос»: «проверяю выпуск», «снимаю копию
  перед обновлением», «ставлю выпуск X».
- История скорости хранится две недели вместо двух суток. Отказы, о которых
  рассказывают через день-другой, теперь есть где посмотреть.
- В истории появился признак связи. Клиент, смотрящий видео, получает всплеск
  раз в двадцать секунд и молчит между ними — на графике это те же нули, что и
  при настоящем обрыве, и отличить их по трафику нельзя. Теперь сервер пишет
  рядом возраст рукопожатия и знает, был ли клиент на связи. Затяжное молчание
  отличается от паузы плеера; двадцатисекундную паузу — по-прежнему нет.
- Описание выпуска в окне обновления больше не рассыпается на абзацы посреди
  фраз.

## 2.10.13

- Кнопка «Проверить обновления» теперь отвечает: пока проверка идёт, пункт меню
  занят, а по её окончании панель говорит словами, вышла ли новая версия, нет
  ли обновлений или проверить не удалось. Раньше при отсутствии новой версии не
  происходило ничего.
- Журнал стал шире, и подробность изменения стоит отдельной строкой — записи
  больше не ломаются посреди фразы. Время проверки в окне состояния служб
  переехало под заголовок.
- Отстающие часы телефона или ноутбука показывали отрицательный возраст:
  «Проверено -34940 сек назад». Исправлено.

## 2.10.12

- График показывал разрыв там, где замер опоздал на секунду: посреди ровной
  загрузки появлялась дырка с надписью «замеров нет». Исправлено.
- В подписях времени и в подсказке при наведении появились секунды.
- Значок щита у клиента, чьи запросы имён идут мимо туннеля, убран: он пугал,
  хотя ничего не было сломано, а сделать с этим на стороне сервера нечего.

## 2.10.11

- График скорости показывает последние десять минут вместо часа: теперь на
  каждый столбец приходится ровно один замер, и провалы видно как есть.
- Подписи под графиком уместились в одну строку рядом со временем, а заливка
  стала одноцветной — второй оттенок читался как посторонняя тень.
- Строка «Предел скорости» в карточке клиента называется «Ограничение
  скорости», а её пустое значение — «Без ограничений».

## 2.10.10

- График скорости стал читаться: отдача рисуется такой же заливкой, что и
  скачивание, а не линией поверх неё, поэтому ряды больше не сливаются в кашу.
  Сам график переехал наверх карточки — за ним её и открывают.

## 2.10.9

- График скорости стал читаться как график, а не как решето: заливка идёт от
  низа сплошь, у неё два оттенка — плотный показывает, сколько было всё время,
  светлый докуда поднималось. Значения переехали вправо, подписи и легенда
  встали по левому краю, а само поле стало темнее окна.

## 2.10.8

- Легенда графика стала понятнее: цветные кружки вместо слов «заливка» и
  «линия», а вместо «к клиенту» и «от него» — «скачивание» и «отдача».

## 2.10.7

- График скорости теперь можно читать. Наведите на него — и увидите время и
  скорость в этой точке, в обе стороны; там, где замеров не было, так и
  написано. Шкала подстраивается под скорость, поэтому всплески больше не
  срезаются о верхний край, а приём и отдача разведены цветом.

## 2.10.6

- График скорости стал читаемым: у него появились ось со значениями, время по
  краям и линии сетки, а приём рисуется сплошной полосой вместо частокола
  штрихов. Шкала больше не задирается одним всплеском, поэтому провалы видно —
  ради них график и заводился.

## 2.10.5

- В карточке клиента появился график скорости за последний час или сутки.
  Теперь на жалобу «вчера вечером не грузило видео» можно посмотреть, а не
  гадать: провалы видны, а время, за которое сервер ничего не записал,
  остаётся пустым и не выдаёт себя за нулевую скорость.

## 2.10.4

- Сервер начал запоминать, что было со скоростью каждого клиента. Пока эту
  память не видно в панели — она нужна для графика, который появится дальше и
  позволит разбирать жалобы вроде «вчера вечером не грузило видео».

## 2.10.3

- Предел скорости у клиента, которому выданы адреса IPv4 и IPv6, работал вдвое
  слабее заданного: очередь заводилась на каждый адрес, и два предела по 50
  Мбит складывались в сто. Теперь предел один на клиента, как и задумано.

## 2.10.2

- Сервер теперь поднимается сам после сбоя туннеля. Раньше интерфейс переживал
  падение службы, и следующий запуск упирался в «уже существует»: связь не
  возвращалась, пока кто-нибудь не зайдёт на сервер руками.

## 2.10.1

- Заданный предел скорости ронял туннель. Исправлено; если вы успели задать
  предел на 2.10.0 и связь пропала, обновление её вернёт.

## 2.10.0

- Клиенту можно задать предел скорости — в его карточке. Предел не ускоряет, а
  сокращает потери: когда сервер отдаёт быстрее, чем выдерживает путь до
  клиента, часть пакетов теряется и шлётся заново, и на замерах впустую уходила
  седьмая часть канала. С пределом потери падают примерно вдвое при той же
  скорости, а вместе с ними уходят задержка и рывки. Заметно на видео, не
  заметно на загрузках.
- Пока предел не задан никому, ничего не меняется.

## 2.9.5

- Панель показывает, чем кончилось обновление. Раньше после удачного она молча
  возвращалась к списку клиентов, и узнать результат было неоткуда. Окно с
  итогом дожидается вас, даже если браузер был закрыт всё это время.
- Перед обновлением видно всё, что изменилось между вашей версией и свежей, а
  не только последняя запись.
- Клиент, которому задали наибольший MTU, получал наименьший. Исправлено.
- Обновление, которое не удалось, больше не может запуститься само.

## 2.9.4

- В меню появилось «Состояние служб»: работает ли резолвер, пишет ли туннель
  свой статус, и когда что-либо перезапускалось и почему. Сервер и раньше сам
  чинил зависшие службы, но узнать об этом можно было только по ssh — о сбое вы
  слышали от пользователей, а не от панели.
- И «Журнал»: кто входил в панель, в том числе неудачно, и что менял. Ключей и
  паролей в журнале нет.

## 2.9.3

- Собственный MTU клиента теперь работает в обе стороны. Раньше он ускорял
  только отдачу: скачивание оставалось урезанным, потому что размер обратного
  направления задавал общий интерфейс сервера. Роутер на проводе получал от
  этого половину выигрыша.
- Клиентам, которым ничего не задано отдельно, ничего не меняется: они
  получают ровно тот же размер, что и раньше.

## 2.9.2

- Крестик на полосе о новом выпуске снова закрывает её. В 2.9.1 он отвечал
  «Не удалось сохранить изменения» и ничего не закрывал.

## 2.9.1

- Панель показывает, что вышел новый выпуск, и обновляет сервер кнопкой.
  «Показать подробности» открывает список изменений и предупреждает, что
  клиенты останутся без связи примерно на минуту. Ssh для обновления больше не
  нужен.
- Полосу можно убрать крестиком до следующего выпуска. Сервер это запомнит, так
  что закрывать её второй раз с телефона не придётся. Напоминание на кнопке
  меню остаётся, пока версия не обновлена.
- В шапке появилось меню, а в нём — «О версиях»: что стоит на сервере, свежая
  ли версия, что несёт туннель, какая система и какой Docker. Раньше на это
  отвечал только ssh.
- Там же «Проверить обновления», если ждать суточной проверки не хочется.

## 2.9.0

- Сервер сам узнаёт, что вышел новый выпуск: раз в сутки спрашивает GitHub и
  запоминает ответ. Раньше о выпуске можно было узнать, только зайдя за ним
  самому, — а выпуски здесь это в том числе починенный обход блокировок.
  Ничего не скачивается и не ставится само.
- Сервер умеет обновиться по запросу, без ssh и повторного запуска мастера.
  Он снимает копию перед обновлением, сверяет исходники выпуска с контрольной
  суммой из его описания, и, если обновление не удалось, возвращает прежний
  выпуск сам.

  **Этот выпуск ставится по-старому, мастером.** Служба, которая делает
  обновление, приезжает вместе с выпуском — значит, привезти её на уже
  работающий сервер надо один раз руками. Дальше обновления пойдут проще.
- Если GitHub у вас недоступен, проверка молча не удастся и жаловаться на это
  каждый день не станет. Отказаться от неё совсем можно флагом
  `--no-update-check`: любая проверка обновлений сообщает GitHub адрес вашего
  сервера, и это ваше решение, а не наша забывчивость.

## 2.8.2

- Обновление сервера, на котором продукт уже стоит, больше не отвергается.
  Правила брандмауэра теперь применяются одинаково на новом сервере и на
  давно работающем.

## 2.8.1

- Отметка «имена разрешаются мимо туннеля» стала точнее: она смотрит на
  последние десять минут вместо получаса и говорит ровно то, что известно —
  «нет запросов к нашему резолверу». Раньше клиент, спросивший нас один раз за
  полчаса, выглядел благополучным, даже если всё остальное время шёл мимо.
- В описании отметки сказано, чего она не видит: за роутером скрыт весь дом, и
  про устройства за ним сервер судить не может.

## 2.8.0

- Панель показывает, у кого из клиентов имена разрешаются мимо туннеля. Это
  бывает, когда DNS настроен на роутере или в браузере: тогда защита от подмены
  ответов не работает, а заметить это было неоткуда.
- В карточке клиента виден его адрес IPv6, а не только IPv4 — тот самый, что
  уже лежит в выданных ему настройках.

## 2.7.1

- Панель объясняет отказы. Раньше недопустимое значение или занятое имя
  отклонялись молча: кнопка нажата, ничего не произошло, причина неизвестна.
- Поле MTU не принимает что попало: границы названы, и кнопка сохранения
  недоступна, пока значение вне них.

## 2.7.0

- MTU можно задать отдельному клиенту. Роутеру на проводе можно поднять его, не
  трогая телефоны, которым нужен запас на мобильную сеть: карточка клиента →
  MTU → «Изменить».
- Установка и обновление заканчиваются проверкой: туннель поднят и несёт свои
  адреса, резолвер отвечает, панель открывается. Если что-то из этого не так,
  установщик скажет об этом сам, а не оставит выяснять потом.
- Раздел «Обновление» в документации переписан. Прежний рецепт скачивал ту же
  версию, которая уже стояла, и хостовую часть не обновлял вовсе.
- В окне QR больше не назван один клиент: конфигурацию читают несколько
  приложений.

## 2.6.0

- **Туннель несёт IPv6.** Это лечит самую частую жалобу: страницы открываются,
  а видео не грузится. Раньше конфигурация клиента забирала себе весь трафик
  IPv6 и вести его было некуда. Включается само, если у сервера есть рабочий
  IPv6; выключается флагом `--no-ipv6`.
- **Защита SSH от перебора паролей.** Любой сервер с белым адресом перебирают
  круглосуточно. Ставится `fail2ban`; порт туннеля не затрагивается.
- **Сторож за службами.** Раз в минуту проверяется, что резолвер отвечает, а
  туннель работает; переставший отвечать перезапускается сам.
- Имена клиентов больше не искажаются переводчиком браузера: список в панели
  показывает ровно то, что записано на сервере.
- Установщик отказывается работать, если образы старше скриптов, — раньше такая
  установка обрывалась на середине.
