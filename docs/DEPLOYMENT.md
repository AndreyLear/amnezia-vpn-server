# Development

## Development environment

Development is performed locally.

## Git

Every logical change must be committed separately.

Do not combine unrelated changes into a single commit.

## Testing

Changes must be tested before committing whenever a testable implementation exists.