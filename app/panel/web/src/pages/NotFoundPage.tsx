export default function NotFoundPage() {
  return (
    <main className="flex min-h-svh items-center justify-center p-6">
      {/* Whole-page message, capitalized (amnezia-vpn-server-4cnf). */}
      <p>Не найдено</p>
    </main>
  );
}
